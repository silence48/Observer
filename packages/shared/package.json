{
	"name": "shared",
	"version": "2.0.0",
	"description": "Shared code between StellarAtlas frontend and backend",
	"type": "module",
	"scripts": {
		"preversion": "pnpm run build",
		"build": "../../node_modules/typescript/bin/tsc  --declaration",
		"post-build": "pnpm run compile-schemas",
		"compile-schemas": "npx ajv compile -s \"schemas/*(network).json\" -r \"schemas/*(node|organization).json\" -o lib/network-schema.js; node generate-dto-validate-functions.mjs",
		"test:unit": "jest"
	},
	"author": "pieterjan84@github",
	"license": "MIT",
	"main": "lib/index.js",
	"types": "lib/index.d.ts",
	"dependencies": {
		"ajv": "^8.20.0"
	},
	"devDependencies": {
		"@types/lodash": "^4.17.24",
		"ajv-cli": "^5.0.0",
		"ajv-formats": "^3.0.1",
		"await-fs": "^1.0.0",
		"lodash": "^4.18.1",
		"typescript-json-schema": "^0.67.4"
	},
	"engines": {
		"node": "26.x"
	}
}
